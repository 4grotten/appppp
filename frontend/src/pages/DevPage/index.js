import React, {Component} from 'react';
import './index.scss';

class DevPage extends Component {
  render() {
    return (
      <React.Fragment>
        {/*<div style={{padding: '350px 50px'}}>*/}
        {/*  <button onClick={() => Notify.success({text:'Вы успешно создали организацию'})}>Toast success</button>*/}
        {/*  <button onClick={() => Notify.info({text:'Вы успешно создали организацию'})}>Toast success</button>*/}
        {/*</div>*/}

        <div style={{marginTop: '50px'}}>
        </div>
      </React.Fragment>
    );
  }
}

export default DevPage;